<script type="text/javascript" src="{{ asset('build/account/app.js') }}"></script>
{#
{% if ctx_page.inbox|default and is_granted('ROLE_USER') %}
    <script src="{{ asset('build/web-push/client.js') }}" data-web-push-client>
        {{ {
            url: path('chang_account_device_subscribe'),
            serverKey: parameter('chang.messenger.vapid.public_key')
        }|json_encode|raw }}
    </script>
    <script type="text/javascript" src="{{ asset('build/msg.js') }}" data-inbox-client>
        {{ {
            url: 'http://127.0.0.1:8899',
            recipient: app.user.recipientHash,
            el: {
                inbox: '[data-inbox]',
                list: '[data-inbox-list]',
                css: 'has-notified',
            },
            sounds: [
                asset('build/sounds/quite-impressed.ogg'),
                asset('build/sounds/quite-impressed.mp3'),
                asset('build/sounds/quite-impressed.m4r'),
            ]
        }|json_encode|raw }}
    </script>
{% endif %}
#}
